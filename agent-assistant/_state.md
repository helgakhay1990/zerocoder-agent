# Состояние проекта: агент-ассистент Zerocoder (оркестр субагентов)

**Обновлено:** 2026-06-05
**Период плана развития:** 29.05.2026 — 28.06.2026 (см. `docs/2026-05-29_plan-razvitiya-30-dney_doc.md`)

---

## Где сейчас

Оркестр собран: главный агент-оркестратор + 15 субагентов в трёх уровнях, knowledge-base из 4 источников правды, чек-листы по 5 жанрам, 3 скилла-методички.

| Слой | Состав | Статус |
|---|---|---|
| Оркестратор | `agent-assistant/CLAUDE.md` | ✓ |
| Субагенты | 15 шт. в `.claude/agents/` (+ `landing-auditor`, 02.06) | ✓ |
| Knowledge-base | `zerocoder-facts`, `audience`, `speakers`, `tone-of-voice` | ✓ все на месте |
| Чек-листы | announce / concept / deck / guide / landing | ✓ все на месте |
| Скиллы | `AI-Copywriter`, `Image-Prompt`, `Webinar-Prep` (нов.) | ✓ |
| Память агентов | `.claude/agent-memory/` — 14 папок | ✓ все на месте |

---

## Неделя 1 (29.05 — 04.06): аудит и точечные доработки

- [x] Доработан `editor` — распознавание регистра по бренду (Zerocoder / IU).
- [x] Добавлен скилл `Webinar-Prep` + подключён в `agent-assistant/CLAUDE.md`.
- [x] `methodist` сослан на `Webinar-Prep` (разведены зоны: содержание vs операционная готовность).
- [x] Аудит субагентов по ссылкам на источники правды — проведён (см. ниже).
- [x] Закрыть найденные пробелы (приоритеты ниже) — `references/`, шаблон `_state`, сверка `speakers.md` закрыты.
- [x] Доработаны ветки по итогам боевых прогонов 02.06 (см. раздел ниже).

### Результат аудита ссылок (29.05)

**Что в порядке:**
- Все ссылки на `knowledge-base/*` — рабочие.
- Все ссылки на `checklists/*` — рабочие.
- Все папки `.claude/agent-memory/<agent>/` существуют.

**Найденные пробелы (по приоритету):**

1. 🟢 **`references/` — наполнено (29.05).**
   *Сделано:* `announces/` — 2 образца (Zerocoder разговорный + IU академический), `concepts/` — 2 образца (Zerocoder вебинар + IU инфо-эфир), `guides/` — 1 образец, `landings/` — 1 образец (блочная структура лендинга Пименова, вычленена из `index.html`). Каждый с шапкой-аннотацией «что/когда/автор/чем хорош».
   *`decks/` — закрыт (04.06).* Собран эталон `references/decks/2026-06_openclaw-praktikum_deck.md` из презентации OpenClaw (62 слайда) в формате `methodist`: ритм-карта дуги продающего вебинара, образцовые слайды по типам, переиспользуемые паттерны. `_README.md` обновлён. Все 5 жанров references теперь наполнены (announces/concepts/guides/landings/decks). Остаток-опция: разговорный/имиджевый эфир без жёсткой продажи (другая дуга) — собрать, когда будет удачный исходник.

2. 🟢 **Шаблон `_state.md` под типовой запуск — заведён (29.05).** `agent-assistant/_state-template.md`: вводные, этапы конвейера по трём форматам (вебинар / акция / интенсив) со ссылками на нужных субагентов, блок источников правды, открытые вопросы, текущая стадия. Копировать под каждый запуск.

3. 🟢 **Явная сверка со `speakers.md` в агентах — добавлена (29.05).** `announce-writer` (шаг 2: регалии только из `speakers.md`, нет спикера → открытый вопрос, не выдумывать) и `methodist` (шаг сверки в гайдах + уточнение в презентациях). Регалии больше не тянутся по памяти и не путаются с `zerocoder-facts.md`.

### Боевые прогоны и доработки веток (02.06–03.06)

**Ветка конкурентов / аналитика (фокус №1) — прогнана, субагенты доработаны.**
- Боевые прогоны 02.06: ТОП-5 вайб-кодинг по цене (`reports/2026-06_vibecoding-top5-price_research.md`), конкуренты Zerocoder (`reports/2026-06_zerocoder-competitors_research.md`), ниша ИИ для юристов (`research/2026-06_lawyers-research_*`).
- В `data-collector`, `comparison-builder`, `insight-extractor`, `analyst-assistant` и скилл `ai-competitors` введены: **лестница доступа до первоисточника** (WebFetch → playwright для Tilda-цен → пометка «не подтверждено»), **уровни уверенности 🟢/🟡/🔴** сквозь весь конвейер, **скрин-пруфы** в `reports/evidence/`, **беспощадный отсев инсайтов до топ-3**.
- ✅ **Запись отчёта субагентом — решено (03.06):** субагент не сохраняет .md сам — это архитектура Claude Code (отдаёт текст, файл пишет оркестратор), не баг. Пробовали Write-glob и абсолютный путь — не матчит; правила откачены сознательно, чинить нечего. Закреплено в `analyst-assistant.md` (шаг 7), памяти аналитика и глобальной памяти. Оркестратор просит у субагента полный текст и сохраняет по формуле именования.

**Ветка аудита страниц (фокус №2) — субагент создан, прогнан.**
- Новый субагент `landing-auditor` (зеркало `landing-architect`: тот строит, этот проверяет), подключён в маршрутизацию `agent-assistant/CLAUDE.md`.
- Боевые аудиты 02.06: главная Zerocoder (`audits/2026-06-02_zerocoder-homepage_audit.md`), курс OpenClaw (`audits/2026-06-02_openclaw-course_audit.md`) — со скрин-пруфами в `audits/evidence/`.

**Ветка монтажа (фокус №3) — БОЕВОЙ ПРОГОН ПРОЙДЕН (03.06), ТЗ одобрено.**
- Реальная запись OpenClaw (2:24, Kinescope `w1DfGxioWo7SJjai77QtVg`) прогнана целиком на новой архитектуре: подмастерьев (`visual-scanner`, `transcript-analyzer`) запускал главный оркестратор, `video-edit-assistant` собирал ТЗ. **Подтверждено: операционный пайплайн (Kinescope-скачивание, ffmpeg-нарезка, whisper) тянет главный оркестратор, не субагент.**
- Итог: `reports/2026-06_webinar-openclaw_tz.md` — одобрено Повелительницей, готово монтажёру. Объём: блюр даты 3 июня, 5 интервалов с точными таймкодами.
- **Калибровка качества (важно):** ТЗ должно давать ПОЛНЫЙ список таймкодов с действиями, монтажёр НЕ ищет сам. Шаг 30 сек мал для коротких событий → внедрён **OCR-проход** (tesseract, нарезка 5 сек, grep дат) для исчерпывающего списка. См. [[project_montage_branch]], [[reference_pshinnik_efir_patterns]].
- Чек-лист `2026-05_montazh-checklist_guide.md`: честные границы (whisper не размечает кашли/«эээ»; даты на экране — `visual-scanner`/OCR; границы реза — по `.json`).
- Открыто: речевая чистка по решению Повелительницы НЕ применяется («остальное оставляем»); whisper на `medium` для 2.4ч душит 8ГБ-маму (убивали и перезапускали) — на больших записях учитывать.

---

## Неделя 2 (05.06 — 11.06): боевой прогон

**Поворот (29.05):** эфиры OpenClaw (3.06) и IU (5.06) Ольга собрала сама — контентный прогон вперёд (концепция → лендинг → анонсы) по ним не нужен. Первый боевой прогон смещаем на **видео-ветку оркестра** (`video-edit-assistant` + `transcript-analyzer` + `visual-scanner`): когда Ольга принесёт запись/ТЗ на монтаж OpenClaw — гоним монтажную ветку как реальную задачу. Контентный сквозной прогон — на следующем вебинаре, который ещё НЕ собран вручную.

- [x] Собрать фактуру по ближайшему вебинару (29.05) → бриф `agent-assistant/Эфир OpenClaw/2026-06-03_webinar-openclaw_brief.md`. Объект: OpenClaw 3 июня. Фактура собрана, 6 открытых вопросов вынесены. *(Эфир собран Ольгой самостоятельно — бриф остаётся как артефакт и источник по открытым вопросам.)*
- [x] **Боевой прогон аналитики и аудита (02.06)** — первыми пошли в бой не видео-, а ветки конкурентов и аудита страниц (см. раздел «Боевые прогоны и доработки веток» в Неделе 1). Дали реальные доработки субагентов и нашли проблему прав Write.
- [x] **Боевой прогон видео-ветки ПРОЙДЕН (03.06):** запись OpenClaw (Kinescope, 2:24) → полный пайплайн → ТЗ `reports/2026-06_webinar-openclaw_tz.md`, одобрено Повелительницей, готово монтажёру. Блюр даты 3 июня — 5 интервалов, полный список через OCR-проход. Подтверждено: операционный пайплайн тянет главный оркестратор. (см. блок «Ветка монтажа» выше)
- [~] Контентный сквозной прогон (`launch-strategist` → … → `Webinar-Prep`) — продвинут 08.06.
  - Этап 1 (концепция) — пройден. **08.06 пришла фактура по Акселератору** (бриф + установочный звонок) → 14 открытых вопросов в основном закрыты, премиса сменилась (продукт продаётся напрямую, эфир продающий 19.06). Каноничная концепция теперь `research/2026-06_webinar-ai-accelerator_concept.md` (полная: нарратив, 4 сегмента ЦА, программа, смыслы под возражения, арсенал данных, воронка/этапы). Старая `concepts/2026-06_akselerator-progrev-webinar_concept.md` помечена ЗАМЕНЁННОЙ (указатель + changelog закрытых вопросов). ⚠️ Честно: содержательную часть концепции Ольга и ассистент собрали вручную (вне оркестра), затем свели в оркестр.
  - Этап 2 (лендинг) — **прогон `landing-architect` пройден 08.06** (через оркестр, по референсу Пименова): `research/2026-06_akselerator-webinar_landing.md`. 13 открытых вопросов вынесены субагентом честно. Это первый реальный прогон субагента в контентной ветке.
  - Этап 3 (анонсы) — **прогон `announce-writer` пройден 08.06** (через оркестр): `research/2026-06_akselerator-webinar_announce.md` (+docx). 3 волны (email + TG-канал + TG-бот), тон «ты». Регалии Кирилла сверены со `speakers.md` — подтверждены; оркестратор дочистил Ё и латиницу (CEO→основатель, A-List→рейтинг визионеров) — субагент их пропустил вопреки самопроверке. ТЗ на лендинги Ольга собрала сама; ТЗ на продукт — Оля + ассистент 09–10.06.
  - Этап 4 (презентация + гайд) — **прогон `methodist` пройден 08.06**: `research/2026-06_akselerator-webinar_deck.md` (30 слайдов) + `..._guide.md` (9 шагов). Этап 5 (редактура) — **прогон `editor` пройден 08.06**: вычистил латиницу/Ё по обновлённому ToV, верифицировал регалии слайда 28 по `speakers.md`.
  - **Итог прогона для ДЗ:** полный pipeline вебинара прогнан через оркестр — **5 субагентов** (launch-strategist контекст + landing-architect + announce-writer + methodist + editor): концепция → лендинг → анонсы → презентация → гайд → редактура. Ретро + находки: `docs/2026-06-08_akselerator-progon-retro_doc.md`. Готово к сдаче 10.06.
  - ⚠️ **Находка прогона:** `landing-architect` справедливо упёрся в устаревший источник правды — `zerocoder-facts.md` помечает Акселератор «в разработке, не продаётся», что опровергнуто фактурой 08.06. Обновить факт-файл. Плюс cases.md пуст (нет кейсов/отзывов), тон «ты» vs ToV «вы» — развилка.
- [x] Замер этапа 1 (concept): выдуманных фактов — **0** (дисциплина «не выдумывать» при нулевой фактуре продукта удержана); открытых вопросов — 14 (честно вынесены); ручных правок текста — минимум (≈1-2, в осн. шлифовка ключевого сообщения), остальное — вводные/решения, которые система корректно запросила, а не насочиняла.
- ✅ **Архитектурная находка прогона — исправлена (03.06):** субагент 2-й ступени НЕ запускает подмастерьев 3-й (нет Task tool). Решение Повелительницы: **главный оркестратор сам дёргает подмастерьев** и передаёт их выход сборщику 2-й ступени. Рефакторинг проведён по всем файлам: главный `CLAUDE.md` (правило + таблица + конвейерные сценарии), 3 сборщика 2-й ступени (`launch-strategist`/`analyst-assistant`/`video-edit-assistant` — режим «приём готового сырья» + соло-fallback), 7 подмастерьев (строка «запускает главный оркестратор»), карта системы (диаграмма + правило уровней). Зафиксировано в памяти `project_orchestra_nested_spawn`. Открытый хвост: операционный пайплайн видео-ветки (скачивание/нарезка/whisper) — проверить на боевом прогоне, кто его реально тянет.

## Неделя 3 (12.06 — 18.06): закрытие узких мест

- [~] Доработать субагентов по итогам прогона — **частично (04.06).** Закрыты дыры покрытия находок прогонов: (1) **OCR-проход** (tesseract по нарезке 5 сек + grep дат) был только в `_state`/ТЗ — внесён в `video-edit-assistant` (третий слой отсмотра) и `visual-scanner` (сверка с OCR-перечнем); раньше следующий монтажный прогон потерял бы находку. (2) Устаревший «открытый вопрос» про операционный пайплайн в `video-edit-assistant` обновлён → подтверждено боевым прогоном 03.06: пайплайн тянет главный оркестратор, + заметка про whisper `medium`/8 ГБ. (3) Write-блокировка дописана в `video-edit-assistant` (файл сохраняет оркестратор) и `comparison-builder`. (4) **OCR-проход добавлен в чек-лист монтажа** `2026-05_montazh-checklist_guide.md` (блок про блюр дат + шаг 4б алгоритма). **Остаток закрыт (04.06):** аудит всех 15 субагентов — Write-нота на месте у всех; висячая ссылка на удалённый транскрипт Пименова в `transcript-analyzer` заменена на OpenClaw; `methodist` теперь ссылается на новый эталон `references/decks/`. Дальнейшие узкие места — по факту следующих прогонов.
- [~] Усилить knowledge-base тем, чего не хватило в бою — **список пробелов собран (04.06):** `docs/2026-06-04_knowledge-base-probely_doc.md` (главный пробел — продукт «Акселератор», ждёт фактуру 08.06; плюс бенчмарки вебинаров, карточка Соболева, приоритет сегментов, отсутствие источника кейсов/отзывов). Наполнение — по мере поступления фактуры/решений.
- [x] Постмонтажная ветка: `video-edit-assistant` на записи эфира — **закрыта (04.06).** Прогнана на OpenClaw (03.06, ТЗ передано монтажёру), плюс собран и обкатан скилл `Montage-TZ` (автосборка черновика ТЗ: транскрипт + OCR по окнам экрана; обкатка — 3,5 мин против ручного вечера). См. `docs/2026-06-04_montage-tz-skill_doc.md`.

## Неделя 4 (19.06 — 28.06): стабилизация и презентация

- [ ] Второй сквозной прогон, сравнить число правок с первым.
- [x] Документ-карта системы — `docs/2026-05-29_orchestra-system-map_doc.md` (вытянута вперёд 29.05, пока ждём запись эфира). Схема 3 уровней + таблица маршрутизации + источники правды + сценарии.
- [x] Демо-прогон для презентации + ретро — **каркас готов (04.06)** + **ретро 1-го прогона (08.06):** `docs/2026-06-08_akselerator-progon-retro_doc.md`. По итогам починено: `tone-of-voice.md` (языковые ограничения + override обращения), `zerocoder-facts.md` (Акселератор/Буткемп выведены из «не продаётся»).
- [x] **Второй сквозной прогон + замер «правки 1-й vs 2-й» (09.06)** — `docs/2026-06-09_orchestra-run2-metric_doc.md`. Прогон `announce-writer` (сырьё) → `editor`, замер по grep. **Результат:** ToV-фикс закрыл смысловой слой сам (субагент без подсказки выдал «ИИ-мышление»/«архитектор цифровой команды», не «AI»); механический мусор (Ё ×10, англицизмы ×6) у генератора остался → снял `editor` (Ё 10→0, англицизмы 6→1, пропустил «вебинаров»). **Метрика зрелости: ручные правки по языку упали с «весь объём» до 1.** Вывод: фикс источника правды и детерминированный гейт editor — дополняют друг друга, не заменяют; финальный человеческий проход всё ещё нужен. Артефакты: `research/2026-06_akselerator-announce-run2-raw.md` (сырьё), `..._announce-run2-edited.md` (после editor).

## Распространение (29.05)

- [x] Подготовлена передача системы коллегам: корневой `.gitignore` (защита секретов и конфиденциального), `.mcp.json.example`, `SETUP.md`.
- [x] Из репозитория исключены секреты (`.mcp.json` с живыми ключами Brave/Kinescope), `settings.local.json`, память агентов, записи/расшифровки эфиров, скриншоты, тяжёлые медиа.
- [x] **Опубликовано в приватном GitHub-репозитории** `helgakhay1990/zerocoder-agent` (https://github.com/helgakhay1990/zerocoder-agent), ветка `main`, 162 файла. Проверено: секреты не ушли, видимость PRIVATE.
- [ ] Выдать коллегам доступ (Settings → Collaborators) — за Повелительницей.

---

## Ближайший шаг

Все три фокус-ветки прогнаны в бою: конкуренты/аналитика + аудит страниц (02.06), видео-монтаж OpenClaw (03.06, ТЗ одобрено). Архитектура оркестра починена (главный оркестратор сам дёргает подмастерьев). Открытые хвосты:
1. **Контентный сквозной прогон — продвинут 08.06.** Фактура пришла, концепция сведена в оркестр (канон — `research/2026-06_webinar-ai-accelerator_concept.md`), `landing-architect` отработал (`research/2026-06_akselerator-webinar_landing.md`). **Следующее:** `announce-writer` ×3 волны под старт ~15.06; ТЗ на продукт — Оля + ассистент 09–10.06; обновить `zerocoder-facts.md` (Акселератор продаётся) и решить тон «ты»/«вы». Цель — довести прогон до анонсов к сдаче ДЗ (среда 10.06).
2. ✅ **ТЗ на монтаж OpenClaw передано монтажёру (04.06).** Финальная версия: `reports/2026-06_webinar-openclaw_tz.md` (+ `.docx` для монтажёра), ссылка на запись Kinescope проставлена, заглушки и открытые вопросы убраны. Речевые правки — не применяем (зафиксировано в ТЗ). Ветка монтажа полностью закрыта.
3. **Речевая чистка эфира OpenClaw** — окончательно НЕ применяется (зафиксировано в ТЗ как решение, не открытый вопрос).

## Бэклог идей (после переезда на новый комп)

См. `docs/2026-06-04_backlog-idei_doc.md`. Порядок: сначала №2, потом №1.
- **№2 (бот↔Montage-TZ) — БОЕВОЙ ПРОГОН ЯДРА ПРОЙДЕН (08.06).** Бот поднят (`launch-pult/.venv/bin/python src/bot.py`, лог `/tmp/pult-bot.log`, gating по ALLOWED_USER_ID), живёт и опрашивает Telegram. Пайплайн `montage_tz.py` прогнан end-to-end на реальном клипе (2 мин из записи OpenClaw, whisper `base`, локальный файл — без Kinescope): ffmpeg → whisper (Metal, ~3 с) → скан речи/OCR → черновик ТЗ `agent-assistant/reports/2026-06_efir_montazh-tz-draft.md`, всё ~14 с. Нашёл 1 тех.заминку; экранный слой пуст (окна шеринга не заданы — для OCR-находок нужен `--windows`). Прогон через КНОПКИ в Telegram сделала Повелительница (08.06) — и он вскрыл реальный баг. Бот-механика отработала штатно (диалог источник→окна→заметки, создание job `jobs/1cfcf637.json`, фон, отлов падения, уведомление). Упал сам монтаж: источник задан ссылкой `https://kinescope.io/<id>`, а `montage_tz.py` (a) не парсил URL — брал его как id целиком, и (b) нет `KINESCOPE_API_TOKEN`. **Починено 08.06:** `resolve_source` теперь извлекает id из ссылки Kinescope (проверено: из URL достаёт `w1DfGxioWo7SJjai77QtVg`), `key` тоже по id (раньше имя кэша было бы битым со слэшами). **Второй прогон (08.06, вечер):** токен вписан в `launch-pult/.env` (gitignored; взят из MCP-конфига, проверен — аккаунт 16418 видео, оба формата id→200). Бот поднят (pid меняется). Прогон на свежей записи `atzUoNZ92CfCrC2ZkzSnHH`: токен ОК, видео найдено, 480p, скачал ~200/324 МБ → `curl (18) transfer closed` (CDN оборвал). **Починено:** скачивание усилено — `--retry 5 --retry-delay 2 --retry-all-errors -C -`, качаем в `.part` с переименованием при успехе (раньше оборванный файл оставался как `out` и кэш-проверка приняла бы его за готовый → whisper по битому). **Третий прогон + диагностика загрузки (08.06):** падало ровно на 200 МиБ — CDN-ссылка (`url`, kinescopecdn.net) режет на 200 МиБ. Поправлено: качаем по `download_link`. Но всплыло: для этой записи `download_link` бывает двух видов — s3.kinescope.io (оригинал, ~90 КБ/с из РФ, ≈час) и msk-*-storage.kinescope.io (транскод 480p/720p, в прямом curl-тесте 0 байт за 6с — нужна особая авторизация/не отвечает отсюда). **Вывод: Kinescope-докачка БОЛЬШИХ файлов из этой среды нестабильна (CDN кап 200 МиБ; storage-ссылки медленные/0 из РФ) — это среда/авторизация ссылок, не баг бота.** Плюс запись-мишень была 2,4 ч (openclaw-web-2) — whisper по ней тяжёл на 8 ГБ в принципе.
**ИТОГ ветки бот↔Montage-TZ:** бот + async-механика + ядро монтажа РАБОТАЮТ (локальный клип → черновик ТЗ за 14 с; диалог/задача/фон/отлов/уведомление прошли вживую). Боевые клики Ольги вскрыли 3 слоя проблем — все, что в коде, починены (парсинг ссылки, ретраи, `.part`-атомарность, `download_link`). **Скачивание добито 09.06** (см. ниже) — большие Kinescope-записи теперь качаются штатно, когда сетевое окно «чистое»; при стабильном обрыве — честная ошибка, не вечный цикл. **Рекомендация: бот использовать с ЛОКАЛЬНЫМ путём к видео** (надёжнее всего) или короткой записью; тяжёлая Kinescope-докачка — best-effort. OCR-слой с `--windows` — остаток.
  - **Скачивание Kinescope — переписано (09.06).** Диагностика на реальной записи `atzUoNZ92CfCrC2ZkzSnHH` (480p, 324 МиБ): сервер `msk-storage` **НЕ поддерживает Range** (`curl: 33 doesn't support byte ranges`), HEAD не отдаёт `Content-Length`. Значит докачать с места обрыва нельзя — `-C -` не просто бесполезен, а валит передачу на непустом `.part`. Старый `--retry --retry-all-errors -C -` рвал и перезапускал с нуля, в плохом сетевом окне → вечный цикл «0→200 МиБ→обрыв». **Новая функция `download_video`** (заменила `resolve_source`-вызов): цикл из ≤6 попыток, каждая качает ЦЕЛИКОМ на свежем соединении (без `-C -`), `--connect-timeout 20 --speed-limit 50000 --speed-time 30` отсекает мёртвый коннект; при стабильном обрыве — `die` с честным «это сеть/CDN РФ↔Kinescope, передай локальный путь». **Проверено боевым прогоном 09.06:** полные 324 МиБ за 44 с, `rc==0`; отдельно проверены ветки «обрыв на 200 МиБ» и «уже готовый `.part`». Подтверждено: потолок 200 МиБ — флап соединения, не лимит URL.
  - `montage_tz.py`: добавлен приём заметок автора — флаги `--notes`/`--notes-file`, разбор по `;`/строкам, вынос таймкода (`split_note`, цены типа «990» таймкодом не считаются), новая секция «Заявлено автором (высокая уверенность)» в начале черновика. Проверено изолированно.
  - `launch-pult/src/montage_jobs.py` (новый): фоновые задачи монтажа. `create_job` — отвязанный процесс (`start_new_session=True`) + `jobs/<id>.json`, лимит 1 за раз; `poll_jobs` — детект готовности по файлу-черновику + reaping зомби через `waitpid` (переживает рестарт бота); `mark_notified`.
  - `launch-pult/src/bot.py`: кнопка «🎬 ТЗ на монтаж» + `/montage`, `ConversationHandler` (источник → окна экрана → заметки), фоновый наблюдатель `_montage_watcher` в `post_init` (раз в 30 с, досылает черновик `send_document` / хвост лога при падении). Бот не висит.
  - Гигиена: `jobs/` в `.gitignore`, `KINESCOPE_API_TOKEN` в `.env.example`.
  - **Проверено:** py_compile + импорт `bot.py`; жизненный цикл задачи на заглушке (done/failed/notified/лимит). **НЕ проверено:** реальный end-to-end на боевой записи (нужен Kinescope id + ffmpeg/whisper/tesseract на Mac) — следующий шаг при запуске бота.
  - Открытый хвост: авто-детект окон показа экрана (пока задаёт человек) — отдельный шаг развития Montage-TZ.
- **№1 (крупный) — АРХИТЕКТУРА СПРОЕКТИРОВАНА (05.06), код не начат.** Design-док `docs/2026-06-05_razvedchik-voronki-arhitektura_doc.md`. Разведка воронки конкурента (регистрация → прогрев → офферы на эфире → постпродажа). Принцип MVP: человек собирает сырьё (редкие шаги), агент синтезирует карту воронки по оси времени.
  - **Решения (05.06):** прогрев — IMAP-чтение scout-почты (агент) + скрины TG-бота вручную; контакт — email+Telegram; подопытный — прямой вайб-кодинг/ИИ-конкурент (имя позже); routines в MVP нет (синтез по требованию).
  - **Строим новое:** субагент `funnel-analyst` (ось времени — цепочка касаний), методичка «Разведка воронки» (чек-листы по слоям + ToS-граница), трекер `_state.md` под конкурента, `scout_inbox.py` (IMAP-сборщик, по образцу montage_tz.py).
  - **Переиспользуем:** `ai-competitors` (статика), `Montage-TZ`/`transcript-analyzer` (эфир-запись уже ищет цены/офферы/дедлайны), аналитический конвейер (уверенность/пруфы).
  - **Этика:** «тайный покупатель» легитимен — один реальный контакт, без фабрикации личностей и обхода капчи/антибота.
  - **Открыто к старту кода:** имя подопытного; scout-почта (провайдер+IMAP+креды в .env); scout TG-аккаунт.
  - **Порядок сборки:** методичка → `funnel-analyst` → трекер-шаблон → `scout_inbox.py` → боевой прогон.

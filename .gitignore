# ── Секреты: НИКОГДА не коммитить ──────────────────────────────
# Содержит живые API-ключи (Brave, Kinescope). Коллеги создают свой из .mcp.json.example
**/.mcp.json

# Локальные/личные настройки Claude Code (у каждого свои)
**/settings.local.json

# ── Личное: память агентов накапливается под конкретного пользователя ──
.claude/agent-memory/

# ── Конфиденциальное: записи и расшифровки эфиров ──────────────
**/transcripts/
agent-assistant/source/

# Скриншоты эфиров и рабочие скрины (кадры записей + личные скриншоты)
screenshots/
/png/

# ── Тяжёлое: модели, видео, презентации, архивы ───────────────
**/models/*.bin
*.mp4
*.pptx
*.pdf
*.zip

# ── Системное / временное ─────────────────────────────────────
.DS_Store
tmp/
.playwright-mcp/

# ── Локальные бэкапы среды (СОДЕРЖАТ СЕКРЕТЫ — никогда в git!) ──
_backups/

# ── Внутренняя база знаний (коммерческие данные Zerocoder — не публиковать) ──
# Нужна агентам локально, но не уходит в публичный репозиторий.
knowledge-base/

# ── Рабочие материалы запусков (концепции/воронки/цены/юр-исследование) ──
# Коммерческое, не для публичного репо. Остаётся локально; сюда же пишут фоновые
# задачи бота (сравнения конкурентов, аудиты).
research/
.learnings/

# Анонсы запусков — коммерческие тексты, остаются локально (как research/)
announces/

# Продающие тексты лендингов/блоков сайта — коммерческое, остаётся локально
landings/
/2026-06_consultation-razbor_announce.docx

# Генерируемые из .md документы (пересобираются скриптом .md_to_docx.py)
reports/*.docx
**/reports/*.docx

# ── Артефакты ДЗ (личное, не часть агента) ────────────────────
homework-card/
homework-submit/

# ── Отдельный проект-MVP со своим git и своим репозиторием ────
/launch-pult/

# ── Личное / ДЗ: контент-эксперименты, не часть агента ────────
/2026-06-02_devochka-robot_image*.jpg
/2026-06_vaybkoding-navigator_concept.*
